# Ring — 仕様書（spec.md）

> **駅を囲む、見えない輪。**
> その内側に自転車を停めると、警告のうえ即日撤去される。
> Ring は、いま自分が立っている場所がその輪の内か外かを答えるブラウザアプリ。

- ドキュメント版数: `1.0.0`
- 最終更新: 2026-08-23
- 状態: 実装対象として確定（本書が実装の唯一の正）

---

## 0. この文書の使い方

本書は **スペック駆動開発（Spec-Driven Development）** の仕様書である。人間と AI の双方が、本書だけを読んで実装判断を下せることを目標とする。

### 0.1 読む順番

| 目的 | 読む章 |
|---|---|
| 何を作るのか知りたい | 1章, 2章 |
| なぜこの設計なのか知りたい | 3章, 6章 |
| データを扱いたい | 5章 |
| 判定ロジックを実装/変更したい | 6章 |
| 画面を実装したい | 7章 |
| Issue を切りたい | 10章 |
| 完成したか判定したい | 11章 |

### 0.2 記法

- **MUST** … 必須。満たさないものは実装完了と見なさない。
- **SHOULD** … 強く推奨。外す場合は `docs/DECISIONS.md` に理由を記録する。
- **MAY** … 任意。
- `FR-x` … 機能要件 (Functional Requirement)
- `NFR-x` … 非機能要件
- `DS-x` … データセット
- `AC-x` … 受け入れ基準 (Acceptance Criteria)

### 0.3 実装の絶対原則（AI 実装者向け）

1. **正しさより誠実さを優先する。** 本アプリは「撤去されるか」を答える。断定できない場所で断定してはならない。確度が低いなら低いと画面に出す。**推測を事実として表示することは、このアプリにおける最大のバグである。**
2. **すべての判定に出典を添える。** 出典 URL と最終確認日を持たないデータを画面に出してはならない（`FR-8`）。
3. **ユーザーに見える文字はすべて日本語。** 技術用語を使わない。エラーは「何が起きて、次に何をすればいいか」を書く。
4. **モバイル縦画面（375×667）が基準。** そこで壊れるものは実装完了ではない。
5. **外部 API に依存しない。** 地図タイル以外のネットワーク依存を増やさない（`NFR-4`）。

---

## 1. プロダクト定義

### 1.1 一言でいうと

「近くの駐輪場」を探すサービスは既にある。Ring はその逆で、**いま自分が立っている場所に停めたら撤去されるのか**を答える。

### 1.2 解決する課題

自転車を路上に停めてよいかは **放置禁止区域の内か外か** で決まる。区域の中なら警告のうえ即日撤去。取り戻すには自転車で 3,000 円前後、原付で 5,000 円前後かかり、平日昼間に保管所まで行く時間が要る。

ところが、この区域がどこなのかを事前に知る手段が事実上ない。

| 課題 | 内容 | Ring の対応 |
|---|---|---|
| **境界が見えない** | 区域境界は道の途中にある。こちら側は大丈夫でも数メートル先はアウト | `FR-2` 境界までの距離をメートル単位で常時表示 |
| **区ごとにバラバラ** | 千代田区は案内図、渋谷区は PDF と地図システム、大田区は区域図。横断して見る方法がない | `FR-1` 全区市町村を単一のデータモデルに統合 |
| **区域が動く** | 渋谷区は原宿・神宮前を 2025-10-01 に、渋谷駅周辺を 2026-04-01 に拡大した | `FR-4` 施行前の区域変更を事前通知 |
| **停めるなで終わる** | 代替がなければ行動が変わらない | `FR-3` 判定と代替提示を必ずセットにする |
| **撤去された後も迷う** | 保管所は区ごとに複数、撤去場所で行き先が変わり、曜日で開所時間が違う | `FR-5` 撤去後の行き先・時間・持ち物を案内 |

### 1.3 既存サービスとの決定的な違い

東京都自身も駐輪場検索を出している。しかしそれは **「安全な場所のリスト」** であって、リストに載っていない場所については何も言えない。

実際に人が停めるのは —— 駐輪場が満車のとき、近くに駐輪場がないとき、5 分だけ停めたいとき —— **つまりリストの外側**である。

**Ring は路上のどこを指しても答えを返す。** これが唯一かつ最大の差別化点であり、この性質を失う設計変更をしてはならない。

### 1.4 ターゲット

東京で自転車を使うすべての人。とくに：

- **土地勘のない場所に出かける人**（初見の街で標識を探す余裕がない）
- **日常的に路上駐輪をしている通勤・通学・買い物の利用者**（いつもの場所が安全とは限らない）

想定端末は **スマートフォンのブラウザ**。ユーザーは非技術者で、路上に立っており、片手はハンドルを持っている。

### 1.5 名前の由来

**Ring**＝ 車輪（自転車）と、駅を囲む見えない輪（放置禁止区域）。
そして、境界に近づいたときに鳴る警告の音。

この 3 つの意味を、プロダクトの視覚言語（`7.7`）に反映する。

---

## 2. 機能要件

### FR-1 停めていいかの判定 【中核】

地図上の任意の 1 点について、放置禁止区域の内か外かを答える。

- **FR-1.1** 地図中央に固定された十字（クロスヘア）が常に判定対象点である。**MUST**
  - タップ選択ではなく、地図を動かすと連続的に判定が更新される方式を採る。理由は `3.2`。
- **FR-1.2** 判定結果は 5 段階（`inside` / `near` / `likely` / `outside` / `unknown`）で返す。定義は `6.2`。**MUST**
- **FR-1.3** 判定は駐輪場のある場所に限定されない。**路上のどこを指しても必ず何らかの結果を返す。** 例外・空表示は許されない。**MUST**
- **FR-1.4** 判定は端末内で完結し、サーバへ座標を送らない。**MUST**（`NFR-5` プライバシー）
- **FR-1.5** 判定結果には確度（`high` / `medium` / `low`）を必ず添える。**MUST**
- **FR-1.6** 地図移動中の判定更新は 1 フレーム（約 16ms）以内に完了する。**SHOULD**

### FR-2 境界の警告 【中核】

区域のすぐ外側は最も危険である。境界は目に見えないため、ユーザーは自分が「ぎりぎり外」にいることを認識できない。

- **FR-2.1** 判定点から最も近い区域境界までの距離をメートル単位で表示する。**MUST**
- **FR-2.2** 区域外かつ境界まで `WARN_DISTANCE_M`（既定 30m）以内のとき、状態を `near` とし警告色で表示する。**MUST**
- **FR-2.3** 判定点から最近傍境界点まで、地図上に線を引き距離ラベルを置く。**MUST**
  - これが「見えない境界を見せる」という本アプリの視覚的中核である。
- **FR-2.4** 区域内にいる場合は、区域外へ出るまでの最短距離も表示する。**SHOULD**
  - 「10m 戻れば安全」という行動可能な情報になるため。

### FR-3 代替の提示 【中核】

「停めるな」で終わらせない。

- **FR-3.1** 判定が `inside` / `near` / `likely` のとき、近い順に駐輪場を最大 5 件提示する。**MUST**
- **FR-3.2** 各駐輪場について、直線距離・収容台数・一時利用料金・利用時間を表示する。**MUST**（データが無い項目は「情報なし」と明示）
- **FR-3.3** 駐輪場を選ぶと地図上に位置が示され、経路アプリへ渡すリンクを出す。**SHOULD**
- **FR-3.4** 判定が `outside` のときも、駐輪場一覧は畳んだ状態で参照できる。**SHOULD**

### FR-4 区域変更の事前通知

区域が拡大される予定を、施行前に知らせる。現地の標識では気づけない情報である。

- **FR-4.1** `status: "planned"` の区域を保持し、施行日（`effective_from`）まで別レイヤで表示する。**MUST**
- **FR-4.2** 判定点が `planned` 区域の内側にある場合、現在の判定に加えて「◯年◯月◯日から撤去対象になります」を表示する。**MUST**
- **FR-4.3** 区域変更の一覧画面を持ち、施行日の近い順に並べる。**MUST**
- **FR-4.4** 施行日を過ぎた `planned` 区域は、ビルド時に `active` へ昇格させる（`scripts/promote-zones.ts`）。**SHOULD**

### FR-5 撤去された後の案内

- **FR-5.1** 撤去された場所（駅または区）を選ぶと、対応する保管所を提示する。**MUST**
- **FR-5.2** 保管所ごとに、住所・地図位置・電話番号・開所曜日・開所時間・休業日・返還手数料・必要な持ち物を表示する。**MUST**
- **FR-5.3** 「今日は開いているか」を端末時刻から判定して表示する。**SHOULD**
  - 保管所は平日昼間しか開いていないことが多く、これが最も知りたい情報のため。
- **FR-5.4** 保管所データが無い区については、区の公式ページへの導線を出す。**MUST**（空振りで終わらせない）

### FR-6 現在地と検索

- **FR-6.1** 起動時に位置情報を要求し、許可されたら現在地へ移動する。**MUST**
- **FR-6.2** 拒否・失敗時は東京駅を初期表示し、理由と次の操作を日本語で案内する。アプリは全機能が使える状態を保つ。**MUST**
- **FR-6.3** 駅名・駐輪場名・区名でのインクリメンタル検索を提供する。**MUST**
- **FR-6.4** 検索は同梱データに対する端末内検索とし、外部ジオコーディング API を使わない。**MUST**（オフライン動作とプライバシーのため）

### FR-7 リスクの可視化

東京都が公表する駅別の放置台数・収容能力から、その駅周辺の「取締りの強さ」を示す。

- **FR-7.1** 最寄り駅の放置台数・収容能力・放置禁止区域の指定有無を表示する。**MUST**
- **FR-7.2** 放置台数を段階表示（多い / 普通 / 少ない）に変換して示す。**SHOULD**
- **FR-7.3** 出典年度（令和7年度）を必ず併記する。**MUST**

### FR-8 根拠の開示 【本アプリの信頼性の中核】

- **FR-8.1** すべての判定カードに、根拠となったデータの出典名・出典 URL・最終確認日・精度区分を表示する。**MUST**
- **FR-8.2** データ出典の一覧画面を持ち、各データセットのライセンスと取得日を示す。**MUST**
- **FR-8.3** 全画面に「最終判断は現地の標識で行ってください」の注意を常設する。**MUST**
- **FR-8.4** 精度が `estimated`（推定）のデータに基づく判定では、「推定」であることを判定カードの主要部に明記する。小さな注釈で済ませてはならない。**MUST**

### FR-9 オープンデータ公開リクエスト生成

放置禁止区域は、区が地図を持っているのに機械可読な形で公開していない。これが本企画最大の障害であり、同時に企画の中身でもある。

- **FR-9.1** 区を選ぶと、その区宛の「放置禁止区域データのオープンデータ公開リクエスト」文面を生成する。**MUST**
- **FR-9.2** 文面には、現在の公開形式（PDF 等）、求める形式（GeoJSON / Shapefile）、根拠（東京都オープンデータ推進の方針）、想定される公益を含める。**MUST**
- **FR-9.3** 生成文面はクリップボードにコピーできる。送信は行わない（ユーザーの行為として残す）。**MUST**

### FR-10 オフライン動作

- **FR-10.1** 一度読み込んだ後は、機内モードでも判定・駐輪場提示・保管所案内が動作する。**MUST**
- **FR-10.2** 地図タイルは取得済み範囲のみ表示し、未取得範囲は「オフラインのため地図を表示できません」と示す。判定自体は継続する。**MUST**
- **FR-10.3** オフライン状態を画面上部に明示する。**MUST**

---

## 3. 設計判断とその理由

本章は「なぜそうしたか」を残す。ここに書かれた判断を覆す場合は `docs/DECISIONS.md` に差分と理由を記録すること。

### 3.1 なぜサーバを持たないか

判定に必要なデータは全東京分でも数 MB に収まる。サーバを持たないことで、

- ユーザーの位置座標が一切外部に出ない（`NFR-5`）
- 通信が切れても動く（`FR-10`）
- GitHub Pages で無料かつ永続的に公開できる
- 運用コストがゼロで、放置しても壊れない

判定は純粋関数であり、サーバに置く理由がない。**この決定は本プロダクトの前提であり、覆さない。**

### 3.2 なぜ「タップして選ぶ」ではなく「中央固定クロスヘア」か

3 案を比較した。

| 案 | 内容 | 却下/採用理由 |
|---|---|---|
| A: タップ選択 | 地図をタップした地点を判定 | 却下。片手操作の親指がタップした場所は画面下部に偏り、判定カードに隠れる。またタップするまで答えが出ない＝「触るまで何も起きない」画面になる |
| B: 現在地のみ | GPS 位置だけを判定 | 却下。「この先に停められる場所があるか」を調べる用途が消える。位置情報を拒否した人が何もできなくなる |
| **C: 中央固定クロスヘア（採用）** | 画面中央の十字が常に判定対象。地図を動かすと連続更新 | **採用。** 起動直後から答えが出ている。指を動かすと数字が変わる＝境界の存在が体感できる。位置情報を拒否しても全機能が使える |

C は「境界を可視化する」という本アプリの目的そのものを操作に埋め込んでいる。**採用理由は操作性ではなく、目的の体現である。**

### 3.3 なぜ放置禁止区域を 2 階層で持つか 【最重要】

調査の結果（2026-08-23 実施）、**放置禁止区域のポリゴンは、東京都オープンデータカタログに機械可読な形で存在しない**ことを確認した。

- カタログ全文検索で「禁止区域」に該当する 125 件を確認したが、自転車の放置禁止区域はゼロ。
- 唯一登録されていた杉並区「自転車放置禁止区域」（すぎナビ, CSV）は、**登録 URL が現在 404**（リンク切れ）。
- 各区は区域を PDF・案内図・独自 GIS の画面としてのみ公開している。

一方で、**東京都都民安全総合対策本部「駅別放置自転車の状況（令和7年度）」の Excel には、駅ごとに「＊＝放置禁止区域に指定」のフラグが入っている。** これは全都 53 区市町村・668 行・702 駅を網羅する公式かつ機械可読なデータである。

そこで区域データを 2 階層で持つ。

| 階層 | 名称 | 内容 | 精度 | カバー範囲 |
|---|---|---|---|---|
| **Tier A** | 区域ポリゴン | 区の公式区域図・条例別表から作図した多角形 | `traced` / `described` | 代表区のみ |
| **Tier B** | 駅指定リング | 都 Excel の指定フラグ＋駅座標から生成する円形の推定区域 | `estimated` | **全都 702 駅** |

判定は Tier A を優先し、Tier A の覆域外でのみ Tier B を使う（`6.3`）。Tier B は必ず「推定」と明示する（`FR-8.4`）。

**この 2 階層構造が、本プロダクトが「代表区のデモ」ではなく「東京全域で答えが返るサービス」である理由である。**

### 3.4 なぜ「わからない」を用意するか

`unknown` を状態として明示的に持つ。データが無い場所で `outside`（＝停めてよい）を返すことは、ユーザーに損害を与えうる。

**判定できないことは失敗ではない。判定できないのに答えることが失敗である。**

### 3.5 技術選定と理由

| 領域 | 採用 | 理由 | 検討した代替 |
|---|---|---|---|
| ビルド | **Vite 7 + TypeScript 5（strict）** | 静的サイトとして出力でき GitHub Pages に直接載る。TS strict は判定ロジックの型安全に必須 | Next.js（サーバ前提の機能が不要で過剰）、素の HTML（データ量とテストに耐えない） |
| UI | **React 19** | 判定状態→画面の対応が宣言的に書ける。ボトムシートの状態遷移が多く、手続き的 DOM 操作では破綻する | Vanilla TS（状態が増えると保守不能）、Svelte（採用理由としては好みの域） |
| 地図 | **MapLibre GL JS v5** | OSS でライセンス制約がない。`raster-*` の paint プロパティで基図の色調をコード側から制御でき、独自の視覚言語を作れる | Leaflet（ラスタ色調制御が CSS filter 依存になり、オーバーレイまで着色されてしまう）、Google Maps（API キーと課金が発生し `NFR-4` に反する） |
| 基図タイル | **地理院タイル（淡色地図）** | 国土地理院の公式タイル。API キー不要・無償・出典明記のみで利用可。日本の道路形状の精度が最も高い | OSM 標準タイル（重負荷利用が規約上グレー）、Mapbox（キーと課金） |
| 地理演算 | **Turf.js（モジュール個別 import）** | 点-多角形内外判定、最近傍線上点、距離、方位をすべて満たす。個別 import でバンドルを最小化できる | 自前実装（境界ケースのバグを埋め込むリスクが高い。判定はこのアプリの生命線） |
| 状態管理 | **Zustand 5** | 判定結果・データ・UI 状態の 3 つを扱うだけ。Redux は過剰、Context は再レンダリング制御が難しい | Redux Toolkit（過剰）、React Context（地図移動時の高頻度更新に不向き） |
| スタイル | **CSS Modules + デザイントークン** | 独自の視覚言語（`7.7`）を作るため、ユーティリティクラスの既定の見た目に引きずられたくない | Tailwind（既定値のまま作ると「よくある AI 製の画面」になる。本件は見た目が価値の一部） |
| テスト | **Vitest** | 判定エンジンは純粋関数であり、境界ケースを網羅的に単体テストできる。Vite と設定を共有できる | Jest（Vite との二重設定が無駄） |
| オフライン | **vite-plugin-pwa（Workbox）** | 路上・地下・電波不良下で使うアプリであり、オフライン動作は装飾ではなく要件（`FR-10`） | 手書き Service Worker（キャッシュ戦略のバグが起きやすい） |
| ETL | **Node + tsx（TypeScript）** | アプリと同一言語で型を共有できる。CP932 の CSV と xlsx を扱う必要がある | Python（型定義がアプリと二重管理になる） |
| 配信 | **GitHub Actions → GitHub Pages** | 無償・永続・リポジトリと一体。ビルド成果物のみを配信する | Vercel（本件はサーバ機能が皆無で利点がない） |

### 3.6 なぜ ETL の出力をリポジトリにコミットするか

調査中、複数の公式データ URL が 404・リダイレクト・curl 拒否を返した（杉並区すぎナビ、台東区 CSV、東京都サイトのホスト移転）。

**ビルド時に外部から取得する設計にすると、区のサイト都合でサイトが壊れる。**

したがって：

- ETL は手動またはスケジュール実行し、**出力 JSON をリポジトリにコミットする**
- CI は生成済みデータをビルドするだけ
- 週次のスケジュール実行で差分を検出し、差分があれば Pull Request を自動作成する（`FR-4` の運用基盤にもなる）

---

## 4. システム構成

```
ブラウザ（すべてここで完結）
├── React 19 + TypeScript（UI）
├── MapLibre GL JS（地図描画）
│   └── 地理院タイル（外部：基図のみ）
├── 判定エンジン（純粋関数 / Turf.js）
└── Service Worker（オフラインキャッシュ）
        ↑ 読み込み
   /data/*.json（静的ファイル・リポジトリに同梱）
        ↑ 生成（手動 / 週次CI）
   scripts/etl/*.ts
        ↑ 取得
   東京都オープンデータカタログ・各区・国土数値情報
```

**ランタイムに自前サーバは存在しない。**

### 4.1 ディレクトリ構成

```
ring/
├── spec.md                    ← 本書（実装の唯一の正）
├── README.md
├── docs/
│   ├── DECISIONS.md           ← 決定台帳（覆せない決定の記録）
│   ├── DATA.md                ← データ出典・ライセンス一覧
│   ├── ZONE-DIGITIZING.md     ← 区域作図の手順と精度基準
│   └── STATUS.md              ← 実装状況（人とAIが現在地を知るための表）
├── explain.html               ← 単一HTML解説ファイル（成果物2）
├── public/
│   ├── data/                  ← ETL 出力（コミット対象）
│   │   ├── zones.geojson
│   │   ├── stations.json
│   │   ├── parkings.geojson
│   │   ├── impounds.geojson
│   │   ├── zone-changes.json
│   │   └── sources.json
│   └── icons/
├── data/
│   └── manual/                ← 手作業で作った一次データ（作図した区域など）
│       ├── zones-traced.geojson
│       └── zone-changes.json
├── scripts/
│   ├── etl/
│   │   ├── fetch.ts           ← 取得（キャッシュ付き）
│   │   ├── stations.ts        ← DS-1 × DS-2 結合
│   │   ├── parkings.ts        ← DS-3..DS-9 統合
│   │   ├── impounds.ts        ← DS-10, DS-11
│   │   ├── zones.ts           ← Tier A/B 生成
│   │   └── index.ts           ← 全実行
│   └── promote-zones.ts       ← planned → active 昇格
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── core/                  ← 判定エンジン（UIに依存しない）
│   │   ├── judge.ts
│   │   ├── geo.ts
│   │   ├── constants.ts
│   │   └── types.ts
│   ├── data/
│   │   └── loader.ts
│   ├── store/
│   │   └── useStore.ts
│   ├── components/
│   │   ├── MapView.tsx
│   │   ├── Crosshair.tsx
│   │   ├── VerdictSheet.tsx
│   │   ├── ParkingList.tsx
│   │   ├── ImpoundGuide.tsx
│   │   ├── ChangeNotice.tsx
│   │   ├── SearchPanel.tsx
│   │   ├── SourcePanel.tsx
│   │   └── RequestGenerator.tsx
│   └── styles/
│       ├── tokens.css
│       └── global.css
├── tests/
│   ├── judge.test.ts
│   └── geo.test.ts
└── .github/workflows/
    ├── deploy.yml
    └── data-refresh.yml
```

---

## 5. データ仕様

### 5.1 採用データセット一覧

調査日 **2026-08-23**。すべて取得・内容確認済み。

| ID | データセット | 提供元 | 形式 | 用途 | 状態 |
|---|---|---|---|---|---|
| **DS-1** | 駅別放置自転車の状況（令和7年度） | 東京都都民安全総合対策本部 | XLSX | **放置禁止区域の指定有無**・放置台数・収容能力 | 取得済(838行) |
| **DS-2** | 国土数値情報 N02 鉄道データ（2024） | 国土交通省 | Shapefile/GeoJSON | 駅の座標 | 取得済(全国10,235) |
| **DS-3** | 自転車駐車場 | 文京区 | CSV | 駐輪場（緯度経度あり） | 取得済(38件) |
| **DS-4** | 区営自転車駐輪場 / 民間自転車駐輪場 | 品川区 | CSV | 駐輪場 | 取得済(26/16件) |
| **DS-5** | 自転車等駐輪場一覧 | 目黒区 | CSV | 駐輪場（料金・時間が最も詳細） | 取得済(49件) |
| **DS-6** | 区営自転車駐車場設置箇所 | 中野区 | CSV | 駐輪場 | 取得済(27件) |
| **DS-7** | 区営二輪車駐車場一覧 | 中央区 | CSV | 二輪車駐車場 | 取得済(13件) |
| **DS-8** | 公営駐輪場一覧 | 世田谷区 | CSV | 駐輪場（**座標欠損あり**） | 取得済(55件/要補完) |
| **DS-9** | 区営自転車駐車場一覧 | 大田区 | XLSX | 駐輪場 | 取得済 |
| **DS-10** | 保管所一覧 | 大田区 | CSV | **撤去駅→保管所の対応**・開所時間 | 取得済(4件) |
| **DS-11** | 区営自転車保管場所設置箇所 | 中野区 | CSV | 保管所・撤去区域の対応 | 取得済(5件) |
| **DS-12** | 駅前放置自転車撤去情報（各駅ごと） | 品川区 | CSV | 駅別の放置実績 | 取得済(28件) |
| **DS-13** | 自転車シェアリングポート | 目黒区 / 港区 | CSV / GeoJSON | 代替手段の提示 | 取得済(59/多数) |
| **DS-14** | 地理院タイル（淡色地図） | 国土地理院 | ラスタタイル | 基図 | 利用可 |
| **DS-15** | 放置禁止区域（作図） | 本プロジェクト | GeoJSON | Tier A ポリゴン | **本プロジェクトで作成** |

**DS-15 が本プロジェクトの独自の貢献である。** 各区が PDF・案内図でしか出していない区域を、出典と精度を明記したうえで機械可読化する。

### 5.2 出力データ形式

すべて `public/data/` に UTF-8 JSON で出力。各ファイルは先頭に `meta` を持つ（GeoJSON は `FeatureCollection` 直下に `meta` を置く）。

```jsonc
"meta": {
  "generated_at": "2026-08-23T00:00:00Z",   // 生成時刻
  "generator": "scripts/etl/zones.ts",
  "spec_version": "1.0.0",
  "license_note": "出典ごとに feature.properties.license を参照"
}
```

#### 5.2.1 `zones.geojson` — 放置禁止区域

`FeatureCollection<Polygon | MultiPolygon>`

| プロパティ | 型 | 必須 | 説明 |
|---|---|---|---|
| `id` | string | ✓ | `zone-{ward}-{seq}` |
| `tier` | `"A"` \| `"B"` | ✓ | A=作図ポリゴン, B=駅指定リング（推定） |
| `name` | string | ✓ | 「渋谷駅周辺放置禁止区域」など |
| `municipality` | string | ✓ | 「渋谷区」 |
| `stations` | string[] | ✓ | 覆う駅名。Tier B 判定の抑止キーになる（`6.3`） |
| `status` | `"active"` \| `"planned"` \| `"expired"` | ✓ | |
| `effective_from` | string (YYYY-MM-DD) | ✓ | 施行日 |
| `effective_to` | string \| null | ✓ | 廃止日 |
| `accuracy` | `"traced"` \| `"described"` \| `"estimated"` | ✓ | 精度区分（`5.3`） |
| `source_name` | string | ✓ | 「渋谷区 放置自転車対策」 |
| `source_url` | string | ✓ | 出典 URL |
| `source_format` | string | ✓ | 「区域図PDF」「条例別表」「都Excel指定フラグ」 |
| `verified_at` | string (YYYY-MM-DD) | ✓ | 最終確認日 |
| `radius_m` | number \| null | | Tier B のみ。推定半径 |
| `notes` | string \| null | | 「原宿・神宮前は2025-10-01に拡大」など |

#### 5.2.2 `stations.json` — 駅マスタ

```ts
interface Station {
  id: string;                 // "st-shibuya"
  name: string;               // "渋谷"
  display_name: string;       // "渋谷駅"
  lon: number; lat: number;   // N02 の駅線分の重心
  lines: string[];            // ["山手線", "埼京線", ...]
  operators: string[];
  municipality: string | null;// DS-1 由来
  designated: boolean | null; // 放置禁止区域の指定有無（DS-1）
  group_label: string | null; // DS-1 の行ラベル（「東京駅、大手町駅、二重橋駅」）
  counts: {                   // 令和7年度・放置台数
    bicycle: number; moped: number; small_moped: number;
    motorcycle: number; total: number;
  } | null;
  capacity: { bicycle: number; moped: number; total: number } | null;
  source: { name: string; url: string; fiscal_year: string; verified_at: string };
}
```

**結合仕様（DS-1 × DS-2）**

1. DS-1 の駅名セルを `、` `,` `・` で分割する
2. 末尾の `駅` を除去する
3. NFKC 正規化し、`ケ`→`ヶ` を統一する
4. DS-2 の `N02_005` と突合する（東京都域 bbox: 経度 138.9–139.95 / 緯度 35.48–35.92）
5. 同名複数路線は座標を平均して 1 駅に集約する
6. 未突合は `data/manual/station-aliases.json` の別名表で解決する

> 実測突合率 **97.0%**（702 トークン中 681 一致）。残りは別名表で吸収する。
> 別名の例：`霞が関`→`霞ケ関`、`二重橋`→`二重橋前`、`都電早稲田`→`早稲田`、`つくばエクスプレス浅草`→`浅草`、`京成江戸川`→`江戸川`、`中央大学`→`中央大学・明星大学`
> 注記行（`municipality` が `（注）`）は除外する。

#### 5.2.3 `parkings.geojson` — 駐輪場

`FeatureCollection<Point>`

| プロパティ | 型 | 説明 |
|---|---|---|
| `id` / `name` / `municipality` / `address` | string | |
| `capacity_bicycle` / `capacity_moped` | number \| null | 収容台数 |
| `fee_temporary` / `fee_monthly` | string \| null | 原文のまま保持（「1回100円」等、正規化しない） |
| `hours` / `note` | string \| null | |
| `operator_type` | `"public"` \| `"private"` \| `"unknown"` | |
| `source_name` / `source_url` / `license` / `verified_at` | string | |

料金は区ごとに表記が全く異なる。**無理に数値化せず原文を出す。** 誤った数値を出すより正確な原文のほうが役に立つ。

#### 5.2.4 `impounds.geojson` — 撤去自転車保管所

`FeatureCollection<Point>`

| プロパティ | 型 | 説明 |
|---|---|---|
| `id` / `name` / `municipality` / `address` | string | |
| `tel` | string \| null | |
| `open_days` | string | 原文（「月曜日～土曜日／第2・第4日曜日」） |
| `open_hours` | string | 原文（「正午～午後8時」） |
| `open_rule` | object \| null | 機械判定用（`5.4`）。無ければ `null` で「今日開いているか」は表示しない |
| `closed_days` | string \| null | |
| `covered_stations` | string[] | この保管所が対応する撤去駅 |
| `covered_area_text` | string \| null | 原文の対応区域 |
| `capacity` | number \| null | |
| `fee_bicycle` / `fee_moped` | string \| null | 返還手数料 |
| `required_items` | string[] | 持ち物（鍵・身分証明書・手数料 等） |
| `source_name` / `source_url` / `verified_at` | string | |

#### 5.2.5 `zone-changes.json` — 区域変更予定

```ts
interface ZoneChange {
  id: string;
  municipality: string;
  title: string;                       // 「原宿・神宮前周辺の放置禁止区域を拡大」
  kind: "expand" | "shrink" | "new" | "abolish";
  effective_from: string;              // "2026-04-01"
  affected_stations: string[];
  summary: string;                     // 非技術者向けの説明
  zone_ids: string[];                  // zones.geojson の planned 区域と対応
  source_name: string; source_url: string;
  published_at: string | null;
  verified_at: string;
}
```

#### 5.2.6 `sources.json` — 出典台帳

`FR-8.2` の出典画面と、応募フォームの「オープンデータの利用状況」の両方がこれを参照する。

```ts
interface Source {
  id: string;                // "DS-1"
  name: string;
  provider: string;          // "東京都都民安全総合対策本部"
  url: string;
  format: string;
  license: string;           // "CC BY 4.0" / "国土数値情報利用約款" 等
  fetched_at: string;
  used_for: string[];        // ["放置禁止区域の指定有無", "駅別放置台数"]
  record_count: number;
}
```

### 5.3 精度区分（`accuracy`）の定義 【必読】

区域データの信頼度を 3 段階で表す。**この区分の意味を変えてはならない。**

| 値 | 定義 | 判定確度 | 画面表示 |
|---|---|---|---|
| `traced` | 区が公開する区域図（PDF・GIS 画面）の境界線を地図上でトレースしたもの。誤差は概ね 10m 以内 | `high` | 「区の区域図から作図」 |
| `described` | 条例別表や区の文章による区域記述（町丁目・道路名）から作図したもの。誤差は道路 1 本分程度 | `medium` | 「区の公開文書から作図」 |
| `estimated` | 都 Excel の「指定あり」フラグと駅座標から機械生成した円。境界の実形状は反映していない | `low` | **「推定です。実際の境界とは異なります」** |

`estimated` の判定を `inside` と断定してはならない。必ず `likely`（可能性が高い）として扱う（`6.2`）。

### 5.4 保管所の開所判定（`open_rule`）

原文の日本語（「月曜日～土曜日／第2・第4日曜日」）は機械判定できない。手作業で構造化したものだけ `open_rule` を持つ。

```ts
interface OpenRule {
  weekdays: number[];              // 0=日 … 6=土
  nth_sunday?: number[];           // [2, 4] = 第2・第4日曜も開所
  start: string;                   // "12:00"
  end: string;                     // "20:00"
  closed_holidays: boolean;        // 祝日休み
  closed_ranges: string[];         // ["12-29..01-03"]
}
```

`open_rule` が `null` の保管所では「今日は開いているか」を表示しない。**推測して表示してはならない**（誤って「開いています」と出すと、ユーザーは無駄足を踏む）。

### 5.5 ライセンスと出典表記

| データ | ライセンス | 必要な表記 |
|---|---|---|
| 東京都オープンデータカタログ由来 | CC BY 4.0（原則） | 提供元名＋データ名 |
| 各区 CSV | 各区のオープンデータ利用規約 | 提供元名＋データ名＋URL |
| 国土数値情報 N02 | 国土数値情報利用約款 | 「国土交通省 国土数値情報（鉄道データ）」 |
| 地理院タイル | 出典明記で利用可 | 「地理院タイル」＋リンク |
| DS-15（本プロジェクト作図） | CC BY 4.0 で再公開 | 精度区分と出典元 PDF を併記 |

出典表記は `FR-8.2` の画面と `README.md` の両方に置く。**MUST**

---

## 6. 判定エンジン仕様 【アプリの心臓部】

`src/core/judge.ts`。**UI に一切依存しない純粋関数**として実装し、Vitest で網羅的にテストする。

### 6.1 型定義

```ts
export type VerdictLevel = 'inside' | 'near' | 'likely' | 'outside' | 'unknown';
export type Confidence  = 'high' | 'medium' | 'low';

export interface Evidence {
  label: string;       // 「渋谷区 放置自転車対策」
  url: string;
  verifiedAt: string;  // "2026-08-23"
  accuracy?: 'traced' | 'described' | 'estimated';
}

export interface Verdict {
  level: VerdictLevel;
  confidence: Confidence;
  headline: string;                 // 「ここは撤去対象です」
  detail: string;                   // 一文の説明
  zone?: Zone;
  distanceToBoundaryM?: number;     // 境界までの距離（m）
  boundaryPoint?: [number, number]; // 最近傍境界点（線を引くため）
  exitDistanceM?: number;           // inside のとき、区域外までの最短距離
  station?: Station;
  distanceToStationM?: number;
  upcoming?: { change: ZoneChange; zone: Zone };  // FR-4.2
  evidence: Evidence[];             // 空配列を許さない
}
```

### 6.2 判定レベルの定義

| level | 意味 | 表示（headline） | 色 |
|---|---|---|---|
| `inside` | Tier A 区域の内側 | **ここは撤去対象です** | 危険色 |
| `near` | 区域外だが境界まで 30m 以内 | **あと ◯m で撤去対象** | 警告色 |
| `likely` | Tier A の覆域外で、指定駅の推定リング内 | **撤去対象の可能性が高い場所です** | 警告色（推定バッジ付） |
| `outside` | いずれの区域にも該当しない | **撤去対象区域ではありません** | 安全色 |
| `unknown` | 判定に必要なデータが無い | **この付近は判定できません** | 中立色 |

`outside` の `detail` には必ず次を含める。**MUST**

> 放置禁止区域ではありませんが、歩行者の通行を妨げる場所や私有地には停められません。

区域外＝どこでも停めてよい、という誤解を生まないため。

### 6.3 判定アルゴリズム

```
judge(point, data, now) -> Verdict

STEP 0. 事前通知の判定（常に実行）
  planned = data.zones.filter(status==='planned' && effective_from > now)
  upcomingHit = planned.find(z => pointInPolygon(point, z))
  → 見つかれば verdict.upcoming に格納（level は上書きしない）

STEP 1. Tier A 区域内の判定
  activeA = data.zones.filter(tier==='A' && status==='active'
                              && effective_from <= now
                              && (effective_to == null || effective_to > now))
  hit = activeA.find(z => pointInPolygon(point, z))
  if hit:
    exit = 区域外への最短距離（境界までの距離）
    return { level:'inside', confidence: hit.accuracy==='traced' ? 'high' : 'medium',
             zone: hit, exitDistanceM: exit, evidence:[hit の出典] }

STEP 2. 境界近傍の判定
  (nearestZone, distM, boundaryPt) = activeA の中で境界が最も近いもの
  if distM <= WARN_DISTANCE_M (=30):
    return { level:'near', confidence: nearestZone.accuracy==='traced'?'high':'medium',
             zone: nearestZone, distanceToBoundaryM: distM,
             boundaryPoint: boundaryPt, evidence:[...] }

STEP 3. Tier B（駅指定リング）へのフォールバック
  ※ Tier A が覆っている駅を除外する（二重判定の防止）
  coveredStations = activeA.flatMap(z => z.stations)  // Set
  candidates = data.stations.filter(s => s.designated === true
                                      && !coveredStations.has(s.name))
  nearest = candidates の中で最も近い駅
  if nearest && distance(point, nearest) <= STATION_RING_M (=200):
    return { level:'likely', confidence:'low', station: nearest,
             distanceToStationM: d,
             evidence:[都Excel の出典（accuracy:'estimated'）] }

STEP 4. 区域外
  if data が当該地域を含む（東京都域内）:
    return { level:'outside', confidence: 近傍に Tier A があれば 'medium' else 'low',
             distanceToBoundaryM: distM, evidence:[...] }
  else:
    return { level:'unknown', confidence:'low',
             detail:'Ring が対応しているのは東京都内です。', evidence:[...] }
```

**STEP 3 の除外規則が最重要。** Tier A ポリゴンで精密に「外」と判定した地点を、Tier B の粗い円で「内かも」と塗り潰してはならない。精度の高いデータが常に勝つ。

### 6.4 定数（`src/core/constants.ts`）

```ts
export const WARN_DISTANCE_M   = 30;   // near と判定する境界からの距離
export const STATION_RING_M    = 200;  // Tier B 推定リングの半径
export const NEARBY_PARKING_N  = 5;    // 提示する駐輪場の最大件数
export const NEARBY_PARKING_M  = 800;  // 駐輪場を探す最大半径
export const TOKYO_BBOX: [number, number, number, number]
  = [138.90, 35.48, 139.95, 35.92];
export const DEFAULT_CENTER: [number, number] = [139.7671, 35.6812]; // 東京駅
```

`STATION_RING_M = 200` の根拠：各区が公開する放置禁止区域図の多くが駅から概ね 100〜300m の範囲を指定していることによる中間値。**この値は推定であり、Tier A の作図が進むにつれて実測に置き換える。** 値を変更する場合は `docs/DECISIONS.md` に根拠を記録する。

### 6.5 距離計算

- 距離は Turf の `distance`（Haversine, `units: 'meters'`）を用いる。東京の緯度における誤差は実用上無視できる。
- 境界までの距離は `polygonToLine` → `pointToLineDistance` を用いる。
- 最近傍境界点は `nearestPointOnLine` で取得する（`FR-2.3` の描画に必要）。
- **区域が多い場合は、まず bbox で粗く絞ってから厳密計算する。** 地図移動のたびに全件計算しない。

### 6.6 テスト要件（`tests/judge.test.ts`）

以下は **MUST**。すべて実データではなく固定のテストフィクスチャで行う。

| # | ケース | 期待 |
|---|---|---|
| T-1 | ポリゴン中心の点 | `inside` |
| T-2 | ポリゴン境界の 1m 外 | `near`, `distanceToBoundaryM ≈ 1` |
| T-3 | 境界の 29m 外 | `near` |
| T-4 | 境界の 31m 外（他に区域なし） | `outside` |
| T-5 | Tier A 覆域内で区域外の点、かつ同じ駅が指定駅 | `outside`（`likely` にならないこと） |
| T-6 | Tier A の無い指定駅から 150m | `likely`, confidence `low` |
| T-7 | Tier A の無い指定駅から 250m | `outside` |
| T-8 | 非指定駅から 50m | `outside` |
| T-9 | 東京都域外（例：横浜） | `unknown` |
| T-10 | `planned` 区域内かつ現在は区域外 | `outside` かつ `upcoming` あり |
| T-11 | `effective_from` が未来の `active` 区域内 | 当該区域を無視すること |
| T-12 | `effective_to` を過ぎた区域内 | 当該区域を無視すること |
| T-13 | 区域データが空 | 例外を投げず `unknown` または `outside` |
| T-14 | 全 verdict | `evidence.length > 0` |
| T-15 | MultiPolygon の穴（内側の穴）にある点 | `inside` にならないこと |

---

## 7. 画面仕様

### 7.1 画面構成

単一ページ。画面遷移はボトムシートとモーダルで表現する。

```
┌─────────────────────────┐
│ [≡]  Ring      [オフライン] │ ← ヘッダ（44px）
│  ┌───────────────────┐  │
│  │                   │  │
│  │       地図         │  │ ← MapLibre（全面）
│  │        ✛          │  │ ← クロスヘア（画面中央固定）
│  │      ╎12m         │  │ ← 境界への線と距離
│  │                   │  │
│  └───────────────────┘  │
│              [現在地]     │
├─────────────────────────┤
│ ━━━                     │ ← ドラッグハンドル
│ ここは撤去対象です          │ ← 判定カード（ボトムシート）
│ 渋谷駅周辺放置禁止区域       │
│ 区域外まで 18m            │
│ ▸ 近くの駐輪場 3件         │
│ ▸ 根拠を見る              │
└─────────────────────────┘
```

### 7.2 ボトムシートの状態

| 状態 | 高さ | 内容 |
|---|---|---|
| `peek` | 約 140px | 判定見出し・区域名・距離 |
| `half` | 画面の 50% | ＋近くの駐輪場・最寄り駅のリスク |
| `full` | 画面の 90% | ＋根拠・出典・撤去後の案内への導線 |

- ドラッグで遷移する。**MUST**
- 判定が `inside` に変わった瞬間は自動で `half` まで上げる。**SHOULD**（重要な情報を見せる）
- 地図操作中はシートを `peek` に下げる。**SHOULD**（地図を隠さない）

### 7.3 判定カードの表示要素

上から順に **MUST**：

1. **状態の見出し**（最大の文字。一目で分かること）
2. **一文の説明**（`detail`）
3. **確度バッジ** — `low` のときは「推定」と明示（`FR-8.4`）
4. **距離** — `inside` なら区域外まで、それ以外は境界まで
5. **区域名 / 最寄り駅名**
6. **事前通知**（`upcoming` があるとき。日付を強調）
7. **近くの駐輪場**（`FR-3`）
8. **根拠**（出典名・URL・最終確認日・精度）
9. **常設注意** — 「最終判断は現地の標識で行ってください」

### 7.4 その他の画面

| 画面 | 起動方法 | 内容 |
|---|---|---|
| 検索 | ヘッダの検索 | 駅名・駐輪場名・区名の絞り込み。選ぶと地図が移動する |
| 撤去されたら | 判定カード内リンク / メニュー | 駅または区を選ぶ → 保管所の詳細（`FR-5`） |
| 区域変更のお知らせ | メニュー | 施行日順の一覧（`FR-4.3`） |
| データについて | メニュー | 出典一覧・ライセンス・精度の説明（`FR-8.2`） |
| 公開リクエスト | データについて画面内 | 区を選んで文面生成（`FR-9`） |

### 7.5 状態別のふるまい

| 状況 | ふるまい |
|---|---|
| 位置情報を拒否 | 東京駅を表示し「位置情報が使えないため東京駅を表示しています。地図を動かして調べたい場所に合わせてください」 |
| 位置情報が取得できない（屋内等） | 同上。再試行ボタンを出す |
| データ読込中 | 地図とスケルトンを出す。「読み込み中」の文言を出す |
| データ読込失敗 | 「データを読み込めませんでした。通信環境を確認して、画面を下に引いて更新してください」＋再試行ボタン |
| オフライン | ヘッダにバッジ。判定は継続。地図未取得範囲は灰色＋説明 |
| 東京都域外 | `unknown`。「Ring が対応しているのは東京都内です」 |

### 7.6 誤操作・異常系の耐性（**MUST**）

- 地図の高速パン・連打 → 判定は最新の 1 件のみ反映（前の計算は破棄）
- 検索欄に空文字・空白のみ・1000 文字 → 落ちない。結果 0 件のときは「該当しません」
- 位置情報の許可を後から変更 → 再取得ボタンで復帰
- 二重起動・バックグラウンド復帰 → 状態を保持
- データの一部欠損（座標が空の駐輪場など）→ その 1 件を除外し、他は表示する。全体を落とさない

### 7.7 視覚言語

このアプリの主題は **「見えない境界を見せる」** こと。デザインはその一点に奉仕する。

| 要素 | 方針 |
|---|---|
| 基図 | 地理院タイル（淡色）を `raster-saturation: -0.9` / `raster-brightness-max: 0.42` で暗いアスファルト色に落とす。情報は残しつつ主役を譲る |
| 区域 | 塗りは低い不透明度。**境界線を強く描く**（太さ 2.5px＋外側に発光）。輪＝ Ring そのものを見せる |
| 予定区域 | 破線の境界。現行区域と明確に区別する |
| クロスヘア | 細い十字。判定レベルに応じて色が変わる。`inside` のときのみ脈打つ |
| 境界への線 | 判定点と最近傍境界点を結ぶ線＋距離ラベル。**この線がプロダクトの象徴である** |
| 危険の表現 | `inside` のとき画面外周に赤いビネットを 1 回明滅。常時点滅させない（うるさい） |
| 書体 | システムの日本語 sans（Hiragino Sans / Noto Sans JP）。数字はやや太く大きく |
| 色 | 危険 `#FF3B3B` / 警告 `#FFB020` / 安全 `#22C08A` / 中立 `#8A93A0`。背景は `#0E1116` 系 |
| 動き | 距離の数字は補間して滑らかに変化させる。値が飛ぶと信用が下がる |

**避けること**：汎用ダッシュボード風の見た目、意味のないグラデーション、絵文字の多用、英語の見出し。

### 7.8 アクセシビリティ

- 主要な文字のコントラスト比 4.5:1 以上。**MUST**
- タップ領域は最小 44×44px。**MUST**
- `prefers-reduced-motion` を尊重し、明滅と補間を止める。**MUST**
- 判定結果は `aria-live="polite"` で読み上げる。**SHOULD**

---

## 8. 非機能要件

| ID | 要件 | 基準 |
|---|---|---|
| **NFR-1** | 初回表示 | 4G 相当で 3 秒以内に地図と判定が出る |
| **NFR-2** | 判定応答 | 地図移動から判定更新まで 100ms 以内（体感で即時） |
| **NFR-3** | バンドルサイズ | 初期 JS（gzip）400KB 以下。データは遅延読込 |
| **NFR-4** | 外部依存 | 実行時の外部通信は地図タイルのみ。API キーを要するサービスを使わない |
| **NFR-5** | プライバシー | 位置座標を外部送信しない。アクセス解析を入れない |
| **NFR-6** | 対応ブラウザ | iOS Safari 16+ / Android Chrome 最新 / デスクトップ主要ブラウザ |
| **NFR-7** | オフライン | 2 回目以降の起動はオフラインで全機能が動く（地図の未取得範囲を除く） |
| **NFR-8** | 再現性 | ETL は同じ入力から同じ出力を生む。生成物は差分レビュー可能な整形 JSON |

---

## 9. 実装ロードマップ

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| **P0 基盤** | リポジトリ・Vite・TS・CI の骨格 | ビルドが通り Pages に空ページが出る |
| **P1 データ** | ETL 実装、`public/data/*` 生成 | 6 ファイルが生成され、件数がログに出る |
| **P2 心臓** | 判定エンジン＋テスト | T-1〜T-15 が全て緑 |
| **P3 地図** | MapLibre・クロスヘア・区域描画 | 地図が出て区域が見える |
| **P4 判定 UI** | ボトムシート・判定カード・境界線 | 地図を動かすと判定が変わる |
| **P5 周辺機能** | 駐輪場・保管所・変更通知・検索 | FR-3, 4, 5, 6 が動く |
| **P6 誠実さ** | 出典表示・注意喚起・公開リクエスト | FR-8, 9 が動く |
| **P7 堅牢化** | PWA・異常系・アクセシビリティ | 7.6 の全項目を実機で確認 |
| **P8 公開** | Pages 公開・解説 HTML | 公開 URL で全機能が動く |

---

## 10. Issue 分割方針

GitHub Issue は本書の `FR-x` / `NFR-x` と 1 対 1 で対応させる。**Issue のタイトルに必ず要件 ID を含める。**

### 10.1 ラベル体系

| 種別 | ラベル | 意味 |
|---|---|---|
| 領域 | `area:data` | ETL・データ整備 |
| | `area:core` | 判定エンジン |
| | `area:ui` | 画面 |
| | `area:infra` | ビルド・CI・配信 |
| | `area:docs` | 文書 |
| 優先度 | `P0-blocker` | これが無いと他が進まない |
| | `P1-core` | 中核機能 |
| | `P2-important` | 重要だが後追い可 |
| | `P3-nice` | あると良い |
| 状態 | `status:done` | 完了 |
| | `status:in-progress` | 着手中 |
| | `status:blocked` | 外部要因で停止 |
| 性質 | `type:feature` / `type:chore` / `type:research` | |

### 10.2 Issue に必ず書く項目

```markdown
## 対応する仕様
spec.md の FR-x

## 完了の条件
- [ ] 検証可能な条件を箇条書き

## 現在の状態
未着手 / 実装中 / 完了

## 備考
```

`docs/STATUS.md` に全 Issue の一覧表を置き、**人も AI も「何ができていて何ができていないか」を 1 画面で把握できる**ようにする。**MUST**

---

## 11. 受け入れ基準

以下を **すべて** 満たしたとき、本仕様は実装完了と見なす。

### AC-1 中核機能

- [ ] 東京都内の任意の地点で判定が返る（駐輪場の有無に依存しない）
- [ ] 境界までの距離がメートルで表示され、地図上に線が引かれる
- [ ] 区域内・境界近傍のとき、近くの駐輪場が距離順に提示される
- [ ] 施行前の区域変更が事前通知として表示される
- [ ] 保管所の場所・開所時間・持ち物が案内される

### AC-2 誠実さ

- [ ] すべての判定カードに出典・最終確認日・精度が表示される
- [ ] 推定に基づく判定に「推定」と明示されている
- [ ] 「最終判断は現地の標識で」が常設されている
- [ ] データの無い場所で `outside` と断定していない

### AC-3 品質

- [ ] `tests/judge.test.ts` の T-1〜T-15 が全て緑
- [ ] 375×667 の画面で全機能が操作でき、横スクロールが発生しない
- [ ] コンソールにエラーが 1 件も出ない
- [ ] 位置情報を拒否しても全機能が使える
- [ ] 機内モードで判定・駐輪場・保管所が動く

### AC-4 成果物

- [ ] GitHub リポジトリが存在し、spec.md と Issue が揃っている
- [ ] `docs/STATUS.md` で実装状況が一覧できる
- [ ] GitHub Pages で公開され、公開 URL で全機能が動く
- [ ] `explain.html` 単体で、作り方と使い方が分かる

---

## 12. 用語

| 用語 | 意味 |
|---|---|
| 放置禁止区域 | 自転車等の放置が禁止され、即時撤去の対象となる区域。区市町村が条例で指定する |
| 即時撤去 | 警告のうえ、その日のうちに撤去されること |
| 保管所 | 撤去された自転車が運ばれる場所。返還には手数料と本人確認が必要 |
| 駅指定リング（Tier B） | 都 Excel の指定フラグから機械生成した推定区域。実際の境界とは異なる |
| 区域図 | 区が公開する放置禁止区域の地図。多くは PDF |
| クロスヘア | 画面中央に固定された判定点の十字マーク |

---

## 13. 将来の拡張（本仕様の範囲外）

- Tier A ポリゴンの全 23 区への拡大（`docs/ZONE-DIGITIZING.md` の手順で追加可能）
- 撤去実施の曜日・時間帯データの収集（区へ開示請求）
- ネイティブアプリ化とバックグラウンド通知（「いま停めた場所が来月から区域になります」）
- 区域データのオープンデータとしての逆提供（`FR-9` の成果を都カタログへ登録申請）

---

**この仕様書は生きている。実装中に得た事実は本書に反映し、覆した決定は `docs/DECISIONS.md` に記録すること。**
